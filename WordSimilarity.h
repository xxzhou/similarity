#ifndef WORD_SIMILARITY_H
#define WORD_SIMILARITY_H
#include<string>
using namespace std;
class WordSimilarity
{
	public:
		WordSimilarity();
		static double calc(const string & a,const string & b);
	private:
};
#endif
